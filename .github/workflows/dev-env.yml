name: Deployment

on:
  push:
    branches:
      - master

jobs:
  DeployDEV:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: deploy
        # ...deployment-specific steps
        run: echo "${{secrets.ENVIRONMENT}}"
        
  DeployPROD:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: deploy
        # ...deployment-specific steps
        run: echo "${{secrets.ENVIRONMENT}}"
